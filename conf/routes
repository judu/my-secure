# This file defines all module routes (Higher priority routes first)
#
# import these routes in the main app as :
# *     /                       module:Secure
#
# ~~~~

GET     /logout                 Secure.doLogout
GET     /secure/logout          Secure.logout
GET     /choice                 Secure.displayChoice

# Auth-specific urls

# Basic Auth
GET     /basic/login            providers.BasicSecurityProvider.login
GET     /basic/logout           providers.BasicSecurityProvider.logout
POST    /basic/login            providers.BasicSecurityProvider.authenticate

# Google Auth
GET     /gapp/login             providers.GoogleSecurityProvider.login
GET     /gapp/logout            providers.GoogleSecurityProvider.logout 
GET     /gapp/authd             providers.GoogleSecurityProvider.finishAuth
